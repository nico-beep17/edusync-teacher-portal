/**
 * Critical Flow Suite — re-exports all spec modules for a single-run entry point.
 *
 * Run individually:
 *   npx playwright test e2e/login.spec.ts
 *   npx playwright test e2e/dashboard.spec.ts
 *   npx playwright test e2e/attendance.spec.ts
 *   npx playwright test e2e/export.spec.ts
 *
 * Or all at once:
 *   npx playwright test e2e/
 */

export {}